.env
volume/*

# macOS metadata
.DS_Store

# Python cache
__pycache__/
*.pyc

# Node (JS asset tests only — runtime app uses no npm packages)
node_modules/

# Orchestrator worker task briefs (scratch, not project artifacts)
TASK_BRIEF_*.md

# Host-side billing store (issue #37) — JSONL samples/state, host-only, never committed
.clihost-billing/
